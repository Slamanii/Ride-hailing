# mobile/ (Expo React Native TypeScript starter)

To run:
1. Install expo-cli (or use npx expo)
2. cd mobile
3. npm install
4. npx expo start

This app includes a simple Account screen (wallet connect placeholder, theme toggle).
